import styled from "styled-components";
import {
  space,
  color,
  background,
  flexbox,
  layout,
  border,
  fontSize,
  textAlign
} from "styled-system";

export const Flex = styled.div`
  display: flex;
  ${space}
  ${color}
  ${background}
  ${flexbox}
  ${layout}
  ${border}
  ${fontSize}
  ${textAlign}
`;
